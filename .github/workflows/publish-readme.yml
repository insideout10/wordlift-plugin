name: wordpress.org

on:
  push:
    branches:
      - master

jobs:
  wordpress:
    name: Publish readme.txt
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v2
      - name: phpunit
        run: ./.github/workflows/publish-readme.sh
