import styled from "styled-components";

const Wrapper = styled.ul`
  padding: 0;
  margin: 0;

  overflow-y: scroll;

  max-height: 200px;
  background: #fff;
  position: absolute;
`;

export default Wrapper;
